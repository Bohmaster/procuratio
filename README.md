# Procuratio Salesforce Mobile backend as a Service || Ionic 3 APP

In order to start up backend:

- Install Node 6.11.2
- npm install on root directory
- slc run

Now browse http://localhost:9000/explorer to see API Explorer